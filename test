#include <stdio.h>

int main(int ac, char *av[]){

        int i;
        printf("コマンドラインに入力された文字列の数%d\n",ac);

        i = 0;
        while (av[i] != NULL){
                printf("%s\n",av[i]);
                i++;
        }

        return 0;
}
